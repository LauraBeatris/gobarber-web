/**
 * @summary The function or code has no effect.
 *
 * @description Can be used on catch blocks to avoid dealing with unexpected errors
 *
 * @example catch(noop)
 */
const noop = (): void => {
  // Do nothing at all
};

export default noop;
